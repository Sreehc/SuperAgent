name: agent-eval

on:
  push:
    branches:
      - main
      - 'codex/**'
  pull_request:

jobs:
  agent-eval:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: pgvector/pgvector:pg16
        env:
          POSTGRES_DB: superagent_test
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: root
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres -d superagent_test"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 10
      neo4j:
        image: neo4j:5.26.3
        env:
          NEO4J_AUTH: neo4j/password
        ports:
          - 7687:7687
          - 7474:7474
        options: >-
          --health-cmd "wget -qO- http://localhost:7474 || exit 1"
          --health-interval 10s
          --health-timeout 10s
          --health-retries 20

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Java 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
          cache: maven

      - name: Setup Python 3
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Start MinIO
        run: |
          docker run -d --name agent-eval-minio \
            -p 9000:9000 \
            -e MINIO_ROOT_USER=minioadmin \
            -e MINIO_ROOT_PASSWORD=minioadmin \
            minio/minio:RELEASE.2024-10-13T13-34-11Z \
            server /data --console-address ":9001"

      - name: Wait for services
        run: |
          until pg_isready -h localhost -U postgres -d superagent_test; do sleep 2; done
          until curl -fsS http://localhost:9000/minio/health/live >/dev/null; do sleep 2; done
          until nc -z localhost 7687; do sleep 2; done

      - name: Run agent-eval
        env:
          TEST_POSTGRES_URL: jdbc:postgresql://localhost:5432/superagent_test
          TEST_POSTGRES_USER: postgres
          TEST_POSTGRES_PASSWORD: root
          POSTGRES_URL: jdbc:postgresql://localhost:5432/superagent_test
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: root
          MINIO_ENDPOINT: http://localhost:9000
          MINIO_ACCESS_KEY: minioadmin
          MINIO_SECRET_KEY: minioadmin
          MINIO_BUCKET: superagent-documents
          MINIO_AUTO_CREATE_BUCKET: 'true'
          NEO4J_URI: bolt://localhost:7687
          NEO4J_USERNAME: neo4j
          NEO4J_PASSWORD: password
        run: python3 scripts/agent_eval.py

      - name: Upload agent-eval summary
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: agent-eval-summary
          path: artifacts/agent-eval-summary.json
